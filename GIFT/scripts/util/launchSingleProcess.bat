@ECHO OFF
::
:: This script file launches GIFT in complete learner mode (running all 8 modules, activemq, and derby).
:: This is just a simple click solution to launching a standard configuration instead of typing it out in the 
:: command line.
::
"%~dp0%launchProcess.bat" start singleprocess