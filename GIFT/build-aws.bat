@ECHO OFF
:: This calls the 'aws' build target which should build everything in GIFT
:: and package it into a zip.  The zip can be started on a GIFT Amazon Web
:: service (AWS) virtual machine (VM).

build.bat aws
